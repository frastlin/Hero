# -*- mode: python -*-
a = Analysis(['fight/engine.py'],
             pathex=['C:\\Users\\brandon\\programming\\PythonFiles\\My_Games\\Hero'],
             hiddenimports=[],
             hookspath=None,
             runtime_hooks=None)
pyz = PYZ(a.pure)
exe = EXE(pyz,
          a.scripts,
          exclude_binaries=True,
          name='engine.exe',
          debug=False,
          strip=None,
          upx=True,
          console=True )
coll = COLLECT(exe,
               a.binaries,
               a.zipfiles,
               a.datas,
               strip=None,
               upx=True,
               name='engine')
